# OS / editor
*~
*.swp
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Build / dist
build/
dist/
*.egg-info/

# Logs / temp
*.log
tmp/
temp/

# Big artifacts / model weights
data/
datasets/
checkpoints/
weights/
*.pth
*.pt
*.ckpt
*.onnx
# NOTE: *.npz is intentionally NOT ignored globally — the pipeline ships and
# relies on small .npz assets (e.g. vae-oid.npz, full_object_pointcloud.npz).
# Ignore only large generated weight archives explicitly if needed.

# Jupyter
.ipynb_checkpoints/

# Per-run scene artifacts (regenerated each run; keep the MX sample in git)
GRiP3_Pipeline/sample_data/real_world/XY/

# OverlapPredator local weights (download separately)
OverlapPredator/weights/
